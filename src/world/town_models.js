// A town precinct: the seven the programme names, built from a town_layout.js
// plan. T1 (docs/mmo/wiring/T1.md) fills this in. Until it does, buildTown
// answers null and site_models falls back to the rolled village, so nothing is
// ever a blank where a town should be.
export function buildTown(/* site, heightAt, opts */) { return null; }
