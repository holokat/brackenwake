// The eleven mega structures of the realms sheet, one per realm, seen from a
// kilometre and climbed. V1 (docs/mmo/wiring/V1.md) fills this in. Until it
// does, buildMegalith answers null and the place stays what it is: ground.
export function buildMegalith(/* site, heightAt, opts */) { return null; }
